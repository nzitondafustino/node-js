<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="/assets/style.css">
    </head>
    <body>
        <% include partials/nav.ejs %>
        <h1>welcome to the prfile of <%= person %></h1>
        <p><strong>Age:</strong><%= data.age %></p>
        <p><strong>Job:</strong><%= data.job %></p>
        <ul>
            <% data.hobbies.forEach(function(item){ %>
               <li><%= item %></li>
            <%}) %>
        </ul>
    </body>
</html>